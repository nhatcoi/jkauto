---
description: Debug loi JKAuto run, step, selector, keyword, API assertion.
argument-hint: "[error/log/test yaml]"
---

Use the `jkauto-run-debugger` skill.

Task: $ARGUMENTS

Find the most likely failure cause first, then provide the smallest concrete fix. If the input includes YAML, preserve the user's structure and only change the failing parts.
