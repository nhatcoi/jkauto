---
description: Chon va kiem tra JKAuto keyword dung runner, dung field.
argument-hint: "[platform/runner] [hanh dong can lam]"
---

Use the `jkauto-keywords` skill.

Task: $ARGUMENTS

Return the exact JKAuto keyword id to use, required `objectRef` / `input` / `expected` fields, and a short valid step example. Do not output raw Playwright, Maestro, Appium, or shell commands unless the user explicitly asks for implementation notes.
