---
description: Viet hoac sua JKAuto test YAML dung format IDE.
argument-hint: "[web/mobile/api] mo ta testcase"
---

Use the `jkauto-testcase-author` skill.

Task: $ARGUMENTS

Create IDE-safe JKAuto testcase YAML. Use supported keyword ids, stable selectors, variables where useful, and keep the output ready to paste into a `.test.yaml` file unless the user asks for explanation.
