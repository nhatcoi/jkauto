{
  "schemaVersion": 1,
  "name": "default",
  "variables": {},
  "api": {
    "baseUrl": "{{BASE_URL}}",
    "defaultHeaders": {},
    "auth": {
      "type": "bearer",
      "token": "{{accessToken}}"
    }
  }
}